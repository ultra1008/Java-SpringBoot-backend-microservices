spring:
  application:
    name: authorization-service
  cloud:
    config:
      uri: http://config-service:8111
      name: authorization-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      preferIpAddress: true
